"use client";

import { EspacePlaceholder } from "@/components/EspacePlaceholder";

export default function FinancementPage() {
  return <EspacePlaceholder titre="Demande de financement" sous="Salons, congrès, formations…" message="Déposer une demande de prise en charge financière (salon, congrès, formation) : objet, montant, justificatifs, validation." />;
}
